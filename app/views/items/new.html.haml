%h1 新規出品

.form
  = form_with model: @item do |f|
    .form-group
      = f.label :name
      = f.text_field :name, placeholder:'商品名', class: 'form-control', id: 'item_name'
    .form-group
      = f.label :description
      = f.text_area :description, placeholder: '商品説明', cols: 50 ,rows: 5, class: 'form-control', id: 'item_description'
    = f.submit '登録する', class: 'submit-btn'